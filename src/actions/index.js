// ============== ACTION TYPES ===============
export const DATA_LOGIN = 'DATA_LOGIN';

// ============== ACTION CREATORS ================
export const dataLogin = (data) => ({
  type: DATA_LOGIN,
  data,
});
